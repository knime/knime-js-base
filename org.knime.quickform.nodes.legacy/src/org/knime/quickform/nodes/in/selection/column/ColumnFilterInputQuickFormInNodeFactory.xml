<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE knimeNode PUBLIC "-//UNIKN//DTD KNIME Node 2.0//EN" "http://www.knime.org/Node.dtd">
<knimeNode icon="quickform_column_filter.png" type="QuickForm">
    <name>Column Filter QuickForm (Comp.)</name>

    <shortDescription>
      Takes a data table and returns an empty data table with only the selected
      columns.
    </shortDescription>

    <fullDescription>
        <intro>
            <p>
              Takes a data table and returns an empty data table with only the 
              selected columns. The selection can also be controlled from a 
              quickfrom (WebPortal).
            </p>
        </intro>
        <option name="Label">
          A descriptive label that will be shown in, for instance the web port.
        </option>
        <option name="Description">
          Some lines of description that will be shown in the remote control 
          form, for instance by means of a tooltip.
        </option>
        <option name="Weight">
          A weight value that influences the position of the component in
          the final layout. Lighter elements will be placed at the top, whereas
          heavier items will be placed nearer the bottom. The value is relative
          to the weights of the other components in the workflow (absolute 
          value is not relevant, only the relative comparison to other
          elements).
        </option>
        <option name="Variable Name">
          Variable identifier.
        </option>
        <option name="Variable Value">
          Variable value containing the select column names.
        </option>
        <option name="Column Filter">
          Column Filter panel with the select include columns.
        </option>
    </fullDescription>

    <ports>
        <inPort index="0" name="Data table">
            A table to filter column(s) from.
        </inPort>
        <outPort index="0" name="Filtered (empty) table">
            The filtered (empty) data table.
        </outPort>
    </ports>
</knimeNode>
